RedLine lab walkthrough

uses volatility3
|Command|Description|
|---|---|
| `windows.malware.malfind` | Finds suspicious executable or injected memory regions |
| `windows.vadinfo` | Displays Virtual Address Descriptor information for process memory |
| `windows.pstree` | Shows process parent-child relationships |
| `windows.filescan` | Finds file objects present in memory |
|`Select-Strings`|To find every url exist in the memory related to attacker ip|
